<?xml version="1.0" encoding="utf-8"?>
<!-- Layout for the SearchHistoryActivity screen, displaying the user's search history -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SearchHistoryActivity"
    android:orientation="vertical"
    android:background="@color/main_screen_content_background">
    <!-- Title for the search history screen -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="SEARCH HISTORY"
        android:gravity="center"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/text_color_primary"
        android:layout_marginTop="20sp" />
    <!-- Button to clear the search history -->
    <Button
        android:id="@+id/btnClearHistory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Clear Search History"
        android:layout_margin="16dp"/>
    <!-- RecyclerView to display the list of search history items -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_recipe"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/recipe_item"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />
</LinearLayout>